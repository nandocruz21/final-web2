// import React, { createContext } from 'react';

/**
 * React Context untuk menyimpan state global (seperti tema, user auth).
 * (Disiapkan untuk pengembangan tingkat lanjut di masa depan)
 */
// export const AuthContext = createContext(null);
