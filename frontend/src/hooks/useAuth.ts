// import { useState, useEffect } from 'react';

/**
 * Custom hook untuk memudahkan pengelolaan state autentikasi di seluruh aplikasi.
 * (Saat ini aplikasi masih menggunakan localStorage sederhana untuk mempermudah pemula)
 */
export const useAuth = () => {
    // const [user, setUser] = useState(null);
    // return { user };
};
